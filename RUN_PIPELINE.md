# Run the full pipeline on a new machine

The complete mentor handoff workflow now lives directly in
[README.md, “Complete mentor workflow”](README.md#complete-mentor-workflow) so the
repository's landing page is the single canonical run guide.

The README covers host prerequisites, the pinned Docker environment, the bundled
judge, probe training, corpus construction, the required one-step GPU smoke test,
all three GRPO routes, checkpoint export, troubleshooting, and verification limits.
